<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brainyoung.ryan.rideoff">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tayo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tayo_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomWebView" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".StationActivity" />
        <activity android:name=".RouteActivity" />
        <activity android:name=".FirstHelp"
            android:theme="@style/Transparent">


        </activity>
    </application>

</manifest>